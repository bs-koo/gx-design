# gx-design v0.4.0 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** gx-design 플러그인에 ① 생성형 프롬프트 규격(PROMPT-PLAYBOOK), ② 코드 렌더링 시각 품질 루프(VISUAL-QUALITY), ③ 전체/핵심 모드 분기를 추가한다.

**Architecture:** 스킬 문서(markdown) 전용 변경 — 코드 없음. 신규 참조 문서 2개를 만들고, 오케스트레이터 SKILL.md 2개와 보조 문서 3개, 방법론 스킬 2개, 에이전트 1개가 이를 참조하도록 수정한다. 스펙: `docs/superpowers/specs/2026-07-24-gx-design-v0.4-design.md`.

**Tech Stack:** Claude Code 플러그인 스킬 문서(markdown), git.

## Global Constraints

- 모든 문서는 한국어로 쓴다(프롬프트 본문·기술 용어는 영어 허용).
- 게이트 3개(브리프 승인·전략 선택·실물 제작)는 어떤 모드에서도 축소하지 않는다.
- 커뮤니티발 미확정 기법은 반드시 `[미검증]` 표기를 붙인다.
- Gemini UI 안내에는 "2026-07 기준" 표기를 남긴다.
- 기존 문서의 톤(간결한 규칙 문장, 표·불릿 위주)을 따른다.
- 작업 브랜치: `feat/v0.4-quality-and-modes` (이미 생성됨). 커밋 메시지는 한국어, 말미에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- 파일 수정 전 반드시 해당 파일을 Read로 읽는다.

---

### Task 1: PROMPT-PLAYBOOK.md 신설

**Files:**
- Create: `skills/gx-design/PROMPT-PLAYBOOK.md`

**Interfaces:**
- Produces: 문서 경로 `skills/gx-design/PROMPT-PLAYBOOK.md`, 섹션 앵커 `## 4. 온스크린 텍스트 정책`, `## 6. 패키지 산출 양식` — Task 3(PRODUCTION-HANDOFF), Task 8(creative-production), Task 9(creative-producer)가 이 경로·섹션명을 참조한다.

- [ ] **Step 1: 파일 작성** — 아래 내용 그대로 생성:

````markdown
# PROMPT-PLAYBOOK — 생성형 이미지·영상 프롬프트 규격

생성형 프롬프트 패키지(PRODUCTION-HANDOFF 경로 B)와 제작 명세의 생성형 프롬프트 절은 이 규격을 따른다.
1순위 타깃은 Gemini 웹 — "이미지 만들기"(Nano Banana 계열)·"동영상 만들기"(Omni·Veo 계열) — 이며, 타 도구 변형은 §5가 다룬다.
근거: Google 공식 프롬프팅 가이드(Veo 3.1·Nano Banana) + 검증된 커뮤니티 관례(출처는 설계 스펙 §11). 커뮤니티발 미확정 기법은 [미검증] 표기.

## 1. Gemini 웹 사용 명세 (2026-07 UI 기준)

- **진입**: Gemini 웹·앱 입력창의 "+" 메뉴 → "이미지 만들기" 또는 "동영상 만들기". Pro 모델 권장. (메뉴명이 바뀌어도 프롬프트 본문은 그대로 유효하다.)
- **템플릿**: 기본은 선택하지 않는다 — 스타일은 프롬프트 본문이 전담한다. 템플릿(단색·시네마틱 등)은 브랜드 무드와 정확히 일치할 때만 지정하고, 지정했다면 패키지에 템플릿명을 명시한다. 템플릿 목록은 UI 업데이트로 바뀔 수 있다 — 이름이 사라졌으면 미선택으로 진행한다.
- **생성 설정값이 정본**: 종횡비·해상도·클립 길이는 프롬프트 문장이 아니라 **UI 설정**(동영상 종횡비 토글 등)으로 지정한다. 프롬프트 문장 속 비율 서술("16:9 wide")은 보조 강조일 뿐 설정을 대체하지 못한다. 컷별 항목의 "생성 설정값" 필드에 별도 기재한다.
- **생성 후**: 이미지는 컷당 3~5장, 영상은 1~2회 생성 → 검수 체크리스트로 선별 → 미달 항목은 **후속 수정 프롬프트**(§2.6)로 같은 대화에서 부분 수정. 재롤(전체 재생성)은 즉시 탈락 기준 위반 시에만.

## 2. 이미지 프롬프트 규격 (Nano Banana 계열)

### 2.1 5요소 뼈대

모든 프롬프트는 아래 5요소를 서술형 문단으로 녹인다(순서 고정 아님, 누락 금지):

1. **Subject** — 무엇을: 구체 명사 + 재질·질감 수식
2. **Action** — 무슨 일이 일어나는가(정물이면 상태 서술)
3. **Location·Context** — 어디서, 어떤 환경·시간대
4. **Composition** — 구도·카메라: 샷 크기, 앵글, 렌즈감(shallow depth of field 등), 여백·오버레이 공간
5. **Style** — 매체·룩: 사진/일러스트/3D, 조명, 색, 무드

### 2.2 구체화 원칙

- 재질·텍스처는 일반명사 대신 소재+질감으로: "armor" → "ornate elven plate armor, etched with silver leaf patterns".
- 조명은 방향·질감까지: "soft window light from the left, gentle falloff".
- 뭉뚱그린 형용사(beautiful, high quality)는 정보가 없다 — 지우고 구체 서술로 바꾼다.

### 2.3 시리즈 일관성 문단 (공통 스타일 블록)

같은 프로젝트의 여러 컷은 공통 문단 하나를 모든 프롬프트 끝에 반복한다:

- 팔레트(hex 포함), 조명 톤, 카메라·필름 룩, 무드 키워드를 한 문단으로 고정
- 마지막 문장: "All images in this series should share this consistent look."
- 생성 도구는 프롬프트 간 맥락을 유지하지 않는다 — 반복 포함이 유일한 일관성 장치다(같은 대화의 후속 수정은 예외).

### 2.4 네거티브 처리

- 금지 요소는 **긍정 서술로 치환**을 우선한다: "no buildings" → "a desolate, empty landscape of bare rock".
- 치환이 불가능한 것만 최소 나열(마스코트·특정 색 등). 나열식 금지어 문단을 프롬프트의 중심으로 만들지 않는다.

### 2.5 참조 이미지

- 참조를 쓰면 역할을 문장으로 고정한다: "Use Image A for the character's pose, Image B for the art style, Image C for the background environment."
- 캐릭터·심볼 일관성이 필요하면 유지 대상 특징을 명시한다: "maintain the exact same facial features — same eyes, nose shape, jawline".

### 2.6 후속 수정 프롬프트 (컷마다 2~3개 필수)

Nano Banana 계열의 최대 강점은 같은 대화에서의 부분 수정이다. 각 컷에 예상 실패 지점을 겨냥한 후속 수정 프롬프트를 2~3개 포함한다:

- **델타 지시**: 바꿀 것 하나만 말한다 — "Change the amber glow to appear in only one place."
- **유지 잠금**: "Keep everything else exactly the same, especially the composition and the palette."
- 전형 용도: 색·조명 보정 / 요소 제거·개수 교정 / 여백 확보(오버레이 자리) / 텍스트 철자 교정

## 3. 영상 프롬프트 규격 (Omni·Veo 계열)

### 3.1 5요소 뼈대

[Cinematography] + [Subject] + [Action] + [Context] + [Style & Ambiance]를 서술형으로 조합한다.

- Cinematography 어휘: 샷(wide/medium/close-up), 무브(static, slow dolly-in, tracking, crane, pan, POV), 렌즈감(35mm, shallow depth of field, wide-angle, macro)
- 멀티샷이 필요하면 타임스탬프로 나눈다: `[00:00-00:02] ...` `[00:02-00:05] ...`

### 3.2 오디오 지시 (전 클립 필수)

오디오를 명시하지 않으면 원치 않는 소리(방청객 웃음 등)가 생성될 수 있다 — 모든 클립에 오디오 지시를 넣는다:

- 대사: 자연어 화자 + 큰따옴표 — `A woman says, "We have to leave now."` 감정은 부사구로: `in a weary voice,`
- 효과음: `SFX: keyboard clicks, distant thunder`
- 배경음: `Ambient noise: quiet office hum`
- 무음 의도면 잠근다: `Ambient noise: near-silent room tone, no music, no audience sounds`
- [미검증] 대사를 콜론 형식(`Character says: "..."`)으로 쓰면 화면 자막 노출이 줄어든다는 커뮤니티 보고가 있다 — 자막이 껴서 나오면 콜론 형식으로 재시도한다.

### 3.3 길이·밀도 규율

- 클립은 8초 한계 기준으로 분할한다(4/6/8초).
- **1클립 1주제 1액션.** 프롬프트는 150~175단어를 넘기지 않는다 — 넘으면 모델이 지시를 선별적으로 무시하기 시작한다.
- 대사는 클립 길이당 중간 길이 문장 1~2개.
- [미검증] POV·셀피 샷은 문장 끝에 "(that's where the camera is)"로 카메라 위치를 못박으면 성공률이 오른다는 보고가 있다.

## 4. 온스크린 텍스트 정책

| 텍스트 유형 | 정책 |
|---|---|
| 정밀 로고·워드마크 | 프롬프트 금지 — 코드 렌더링 전담, 생성 이미지 위 합성 |
| 긴 카피·문장(영문 25자+, 한글 문장) | 프롬프트 금지 — 편집 도구 후삽입 |
| 짧은 영문 단어·숫자(1~10자) | 허용 — 따옴표로 문구 명시 + 폰트·굵기·색·위치를 문장으로: "The headline 'LAUNCH' rendered in bold white sans-serif at the top". 실측(비공식): 1~10자 정확도 약 98%, 25자 초과 시 60%대로 급락 |
| 한글 짧은 단어(8~10자 이내) | [미검증] 시도 가능 — 생성 후 철자 검수 필수, 1회 수정 실패 시 후삽입 폴백 |

- 텍스트가 이미지의 주체(포스터 헤드라인 등)면 **2단계**로 간다: ① 카피 문구 확정 → ② 그 문구의 정확한 렌더를 요구하는 프롬프트 작성.
- 텍스트를 넣지 않는 컷은 §2.4 원칙대로 긍정 서술로 잠근다: "no readable text" 대신 "all detail abstract and soft, nothing reads as text".

## 5. 타 도구 변형

- **모델 무관 서술형**: §2·§3의 서술형 본문은 특정 문법 없이 대부분의 웹 UI 도구(DALL-E, Flux 등)에 그대로 쓸 수 있다. 타 도구용 본문이 따로 필요하면 설정값 서술(비율 등)만 문장으로 복원해 제공한다.
- **파라미터형 도구(Midjourney 등)**: 5요소 본문은 유지하되 ① 비율·스타일 강도는 도구 파라미터로 분리(예: `--ar 16:9`), ② 네거티브는 도구 전용 필드 사용, ③ 시리즈 일관성 문단은 스타일 참조 기능(sref 등)으로 대체 가능함을 안내한다.

## 6. 패키지 산출 양식

저장: `outputs/final/YYYY-MM-DD_<project>_gemini-prompts.md`

1. **사용법** — 표(컷 | 용도 | 진입 메뉴 | 템플릿 | 생성 설정값) + 생성 후 처리 흐름(§1). 템플릿 열의 기본값은 "미선택".
2. **공통 스타일 블록** — §2.3 시리즈 일관성 문단(복붙용 코드블록).
3. **컷별 항목** — 각 컷마다: 목적(한국어 1줄, 명세의 절·시간 구간 인용) / 생성 설정값(UI로 지정할 값) / PROMPT(복붙용, 5요소 + 공통 블록 포함) / 후속 수정 프롬프트 2~3개(§2.6) / 한국어 해설 / 검수 포인트 / 실패 시 변주 1개(단순화 방향).
4. **합본 가이드** — 로고 합성·타이틀 조판·클립 연결 규칙(해당 시).
5. **검수 체크리스트** — 즉시 탈락 기준(브랜드 금지 요소·텍스트 오렌더 등) + QA 4항목(AI 아티팩트 / 기술 정확성 / 구성 문제 / 스타일 일관성) + 컷별 검수 포인트 통과.
````

- [ ] **Step 2: 검증**

Run: `grep -n "## 4. 온스크린 텍스트 정책\|## 6. 패키지 산출 양식\|\[미검증\]" skills/gx-design/PROMPT-PLAYBOOK.md | head -8`
Expected: `## 4.`·`## 6.` 헤딩 각 1건 + `[미검증]` 4건 이상 매치.

- [ ] **Step 3: 커밋**

```bash
git add skills/gx-design/PROMPT-PLAYBOOK.md
git commit -m "feat: PROMPT-PLAYBOOK 신설 — Gemini 웹 타깃 생성형 프롬프트 규격

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: VISUAL-QUALITY.md 신설

**Files:**
- Create: `skills/gx-design/VISUAL-QUALITY.md`

**Interfaces:**
- Produces: 문서 경로 `skills/gx-design/VISUAL-QUALITY.md`, "렌더 품질 로그" 개념 — Task 3(PRODUCTION-HANDOFF)·Task 6(REVIEW-AXES)·Task 9(creative-producer)가 참조.

- [ ] **Step 1: 파일 작성** — 아래 내용 그대로 생성:

````markdown
# VISUAL-QUALITY — 코드 렌더링 시각 품질 루프

문서 검수만으로는 실물 품질을 보증할 수 없다 — 렌더된 이미지를 직접 열어 보고 비평하는 것이 이 문서의 전부다.
적용 대상: PRODUCTION-HANDOFF 경로 A(코드 렌더링)와 UI 프로토타입 등 이미지·화면 실물이 나오는 모든 제작. 모드(전체/핵심)와 무관하게 의무다.

## 절차

1. **제작 전**: frontend-design 스킬이 가용하면 로드한다.
2. **대표 컷 렌더**: 전체 렌더 전에 대표 컷(스틸·대표 화면) 2~3장을 먼저 렌더한다.
3. **시각 비평(핵심)**: 렌더 이미지를 Read로 직접 열어 보고 아래 기준으로 비평한다. 명세 값 대조가 아니라 디자인 비평이다.
   - **시각 위계**: 시선이 의도한 순서로 흐르는가
   - **간격·정렬**: 리듬이 일관적인가, 어색한 붙음·벌어짐은 없는가
   - **밀도**: 빈약해 허전하거나 과밀하지 않은가
   - **색**: 균형·대비가 충분한가, 팔레트를 벗어난 색은 없는가
   - **정체성 오독**: 다른 것으로 읽히지 않는가 — "이 심볼이 막대 차트로 보이지는 않는가", "이 키 비주얼이 미완성 와이어프레임으로 보이지는 않는가"를 명시적으로 자문한다
   - **QA 4항목**: AI 아티팩트·렌더 결함(폰트 폴백 흔적, 깨짐) / 기술 정확성(치수·해상도·hex) / 구성 문제(잘림·오버랩) / 스타일 일관성(다른 자산과)
4. **수정 → 재렌더 → 재비평**: 루프는 **최소 1회, 최대 3회**. 회차마다 비평 요지(발견 → 수정)를 제작 문서의 "렌더 품질 로그" 절에 기록한다.
5. **통과**: 자체 비평 Critical 0건이면 전체 렌더로 진행한다.
6. **3회 초과**: Critical이 남으면 자동 진행하지 않는다 — 사용자에게 한계와 대안(경로 변경·수동 보정·명세 조정)을 보고한다.

## 검수 연계

- 검수(REVIEW-AXES) 축 B의 입력에 실물 이미지 경로와 렌더 품질 로그를 포함한다.
- 루프 기록이 없는 실물은 검수에서 Critical("품질 루프 미수행")로 처리한다.

## Red Flags

- 렌더 파일을 저장만 하고 열어 보지 않았다
- "명세 값과 일치한다"만 확인하고 통과시켰다 — 값이 맞아도 조잡할 수 있다. 비평 기준은 절차 3의 6가지다
- 렌더 품질 로그 없이 outputs/final/에 실물을 저장했다
````

- [ ] **Step 2: 검증**

Run: `grep -n "최소 1회, 최대 3회\|정체성 오독\|렌더 품질 로그" skills/gx-design/VISUAL-QUALITY.md`
Expected: 각 1건 이상 매치.

- [ ] **Step 3: 커밋**

```bash
git add skills/gx-design/VISUAL-QUALITY.md
git commit -m "feat: VISUAL-QUALITY 신설 — 코드 렌더링 시각 품질 루프 의무화

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: PRODUCTION-HANDOFF.md 수정 — 신규 문서 연결

**Files:**
- Modify: `skills/gx-design/PRODUCTION-HANDOFF.md`

**Interfaces:**
- Consumes: Task 1의 `PROMPT-PLAYBOOK.md`, Task 2의 `VISUAL-QUALITY.md`.

- [ ] **Step 1: 경로 A에 품질 루프 연결** — Edit(old → new):

찾기:
```
- 전체 렌더 전에 대표 컷 2~3장을 스틸로 먼저 렌더해 명세와 대조한다.
```
바꾸기:
```
- 렌더 전후 [VISUAL-QUALITY.md](VISUAL-QUALITY.md)의 시각 품질 루프를 수행한다 — 대표 컷 2~3장을 먼저 렌더해 직접 열어 보고 비평·수정하며(최소 1회·최대 3회), 통과 후 전체 렌더로 간다.
```

- [ ] **Step 2: 경로 B를 PROMPT-PLAYBOOK 참조로 교체** — "## 3. 경로 B" 절 전체(`creative-producer에게 위임한다(final 명세 경로 + 아래 구조를...` 부터 `- 프롬프트 본문은 영어가 기본(생성 안정성), 사용자가 원하면 한국어 본문으로 쓴다.` 까지)를 아래로 교체:

```markdown
creative-producer에게 위임한다. 위임 프롬프트에 final 명세 경로와 [PROMPT-PLAYBOOK.md](PROMPT-PLAYBOOK.md) 경로를 함께 전달하고, 패키지 구조·프롬프트 규격·텍스트 정책은 그 문서를 정본으로 따르게 한다.
저장: `outputs/final/YYYY-MM-DD_<project>_gemini-prompts.md`

핵심 원칙 요약(상세와 예외는 PROMPT-PLAYBOOK):

- 생성 설정값(종횡비·해상도·클립 길이)은 프롬프트 문장이 아니라 UI 설정으로 분리 안내한다.
- 컷마다 후속 수정 프롬프트 2~3개를 포함한다 — 재롤이 아니라 같은 대화의 부분 수정이 기본 경로다.
- 온스크린 텍스트는 PROMPT-PLAYBOOK §4 정책을 따른다(정밀 로고·한글 문장은 후삽입, 짧은 영문 1~10자는 허용).
- 영상은 클립당 8초 한계로 분할하고 모든 클립에 오디오 지시를 넣는다.
- 프롬프트 본문은 영어가 기본(생성 안정성), 사용자가 원하면 한국어로 쓴다.
```

- [ ] **Step 3: 검증**

Run: `grep -n "PROMPT-PLAYBOOK\|VISUAL-QUALITY" skills/gx-design/PRODUCTION-HANDOFF.md`
Expected: VISUAL-QUALITY 1건(경로 A), PROMPT-PLAYBOOK 3건 이상(경로 B). 그리고 `grep -c "온스크린 텍스트는 프롬프트에 넣지 않는다" skills/gx-design/PRODUCTION-HANDOFF.md` → 0.

- [ ] **Step 4: 커밋**

```bash
git add skills/gx-design/PRODUCTION-HANDOFF.md
git commit -m "feat: 실물 제작 게이트에 PROMPT-PLAYBOOK·VISUAL-QUALITY 연결

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: gx-design SKILL.md — 모드 분기

**Files:**
- Modify: `skills/gx-design/SKILL.md`

**Interfaces:**
- Produces: "모드 결정" 절과 단계별 "**핵심 모드**:" 조항 패턴 — Task 5(BRIEF-INTERVIEW)·Task 7(gx-redesign)이 같은 패턴을 따른다.

- [ ] **Step 1: 모드 결정 절 삽입** — 도입부 문장 `각 단계는 완료 기준을 충족했을 때만 통과한다. 단계를 줄이고 싶으면 건너뛰지 말고 사용자에게 축소안을 제안해 승인받는다.` 바로 뒤에 삽입:

```markdown

## 모드 결정

- 스킬 인자에 "핵심"·"빠르게"·"경량"이 있으면 **핵심 모드**, "전체"·"풀"이 있으면 **전체 모드**로 시작한다.
- 인자에 없으면 단계 0 첫 라운드의 첫 문항으로 모드를 묻는다(header "진행 모드", 옵션 2개 — 산출물 범위가 넓거나 이해관계자가 많으면 전체에, 단일 산출물·마감 촉박이면 핵심에 "(추천)" 표기).
- 결정된 모드를 브리프에 기록하고, 단계 보고마다 현재 모드를 표기한다.
- 전체 모드는 아래 각 단계를 그대로 수행한다. 핵심 모드는 각 단계의 **핵심 모드** 조항을 따른다.
- **게이트(브리프 승인·전략 선택·실물 제작)는 모드와 무관하게 유지된다.** 핵심 모드가 줄이는 것은 내부 작업량뿐이다.
- 사용자가 도중에 전환을 요구하면 전환한다 — 이미 통과한 게이트는 다시 열지 않고, 남은 단계만 새 모드 규칙을 적용한다.
```

- [ ] **Step 2: 단계별 핵심 모드 조항 추가** — 각 단계의 "완료 기준:" 줄 **바로 앞**에 아래 줄을 각각 삽입:

단계 0:
```
**핵심 모드**: 인터뷰를 1라운드(최대 4문항, 상류 결정 우선)로 제한하고 미해소 결정은 [가정]으로 채운다 — BRIEF-INTERVIEW.md의 핵심 모드 조항 참조. 승인 게이트는 동일하다.
```
단계 1:
```
**핵심 모드**: design-researcher 1개만 스폰해 통합 리포트 1개(상한 100줄)를 만든다. 조사 축 병렬 분리는 하지 않고, 웹 조사는 전략 결정에 직접 필요한 것만 한다.
```
단계 2:
```
**핵심 모드**: DESIGN-IT-TWICE 대신 design-strategist 1개에게 주 콘셉트 1안(design-strategy 콘셉트 형식 준수) + 반대 방향 미니 대안(10줄 요약)을 작성시킨다. AskUserQuestion 선택 게이트는 유지 — 옵션: 주안 채택(추천) / 대안 확장(재위임) / 방향 수정. 전략 근거가 부족하면 보충 리서치 1회(상한 100줄)를 스폰할 수 있다 — 무단 전체 모드 전환은 금지.
```
단계 3:
```
**핵심 모드**: 위임 프롬프트에 분량 상한 200줄과 매체별 필수 섹션(creative-production 스킬 기준)을 명시한다.
```
단계 4:
```
**핵심 모드**: REVIEW-AXES.md의 핵심 모드 조항을 따른다 — 검수 서브에이전트 1개가 축 A→축 B를 순차 점검하되 보고는 축별 분리를 유지하고, Critical만 수정 루프를 돈다.
```
단계 5:
```
**핵심 모드**: 경량화하지 않는다 — 전체 모드와 동일하게 수행한다(실물 품질이 이 파이프라인의 최종 가치다).
```

- [ ] **Step 3: 합리화 표에 행 추가** — 표 마지막 행(`| "md 명세가 곧 결과물이다" | ...`) 뒤에 추가:

```
| "핵심 모드니까 게이트도 줄이자" | 핵심 모드가 줄이는 것은 내부 작업량이다. 게이트 3개는 모드와 무관하게 유지된다. |
```

- [ ] **Step 4: 검증**

Run: `grep -c "핵심 모드" skills/gx-design/SKILL.md`
Expected: 10 이상. Run: `grep -n "## 모드 결정" skills/gx-design/SKILL.md` → 1건, 위치는 "## 단계 0" 앞.

- [ ] **Step 5: 커밋**

```bash
git add skills/gx-design/SKILL.md
git commit -m "feat: gx-design에 전체/핵심 모드 분기 추가

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: BRIEF-INTERVIEW.md — 핵심 모드 조항

**Files:**
- Modify: `skills/gx-design/BRIEF-INTERVIEW.md`

**Interfaces:**
- Consumes: Task 4의 "진행 모드" 문항 규칙.
- Produces: "## 핵심 모드 조항" 섹션 — gx-redesign REDESIGN-INTERVIEW.md는 3~7절 준용 규칙으로 자동 상속하므로 별도 수정 불필요.

- [ ] **Step 1: 문서 끝(브리프 양식 절 뒤)에 섹션 추가:**

```markdown

## 핵심 모드 조항

- 모드가 인자로 정해지지 않았으면 첫 라운드의 첫 문항으로 진행 모드(전체/핵심)를 묻는다(header "진행 모드").
- 핵심 모드에서는 라운드를 1회로 제한한다: frontier에서 상류 결정 순으로 최대 4문항(모드 문항이 포함된 라운드라면 상류 결정 3문항)만 묻고, 남은 미해소 결정은 합리적 기본값으로 채워 브리프에 [가정] 표기한다.
- 종료 게이트(브리프 전문 제시 → 승인)는 모드와 무관하게 동일하게 수행한다.
- 사용자가 모드 문항에 답하기 전에 탈출구("그만, 진행해")를 발동하면 핵심 모드로 간주하고 [가정] 목록에 기록한다.
```

- [ ] **Step 2: 검증**

Run: `grep -n "## 핵심 모드 조항" skills/gx-design/BRIEF-INTERVIEW.md`
Expected: 1건.

- [ ] **Step 3: 커밋**

```bash
git add skills/gx-design/BRIEF-INTERVIEW.md
git commit -m "feat: 브리프 인터뷰에 핵심 모드 1라운드 조항 추가

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: REVIEW-AXES.md — 실물 검수 입력 + 핵심 모드

**Files:**
- Modify: `skills/gx-design/REVIEW-AXES.md`

**Interfaces:**
- Consumes: Task 2의 렌더 품질 로그 개념.

- [ ] **Step 1: 축 B 입력 확장** — Edit(old → new):

찾기:
```
   - **축 B — 완성도**: 입력 = 산출물(+ 매체 정보). 검수 범위 = 정보 구조, 시각 완성도, UX/UI 또는 영상, 제작 가능성.
```
바꾸기:
```
   - **축 B — 완성도**: 입력 = 산출물 + 실물 이미지·렌더 결과 경로(존재 시 필수 — VISUAL-QUALITY.md의 렌더 품질 로그 포함) + 매체 정보. 검수 범위 = 정보 구조, 시각 완성도(실물 이미지를 직접 열어 확인), UX/UI 또는 영상, 제작 가능성.
```

- [ ] **Step 2: 문서 끝에 핵심 모드 조항 추가:**

```markdown

## 핵심 모드 조항

핵심 모드에서는 검수 서브에이전트 1개가 축 A → 축 B를 순차 수행한다. 두 축의 보고는 여전히 분리 작성하고 합치지 않는다. Critical만 수정 루프를 돌리고(수정 후 해당 축만 재점검), Major 이하는 기록만 남긴다.
```

- [ ] **Step 3: 검증**

Run: `grep -n "VISUAL-QUALITY\|## 핵심 모드 조항" skills/gx-design/REVIEW-AXES.md`
Expected: 각 1건.

- [ ] **Step 4: 커밋**

```bash
git add skills/gx-design/REVIEW-AXES.md
git commit -m "feat: 검수 축 B에 실물 이미지 입력 의무화 + 핵심 모드 통합 검수

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: gx-redesign SKILL.md — 모드 분기

**Files:**
- Modify: `skills/gx-redesign/SKILL.md`

**Interfaces:**
- Consumes: Task 4의 모드 결정 절 패턴, Task 5·6의 핵심 모드 조항.

- [ ] **Step 1: 모드 결정 절 삽입** — 도입부 `각 단계는 완료 기준을 충족했을 때만 통과한다.` 바로 뒤에 삽입 (Task 4 Step 1과 동일한 본문을 쓰되, 첫 불릿의 "단계 0 첫 라운드"는 그대로 유효):

```markdown

## 모드 결정

- 스킬 인자에 "핵심"·"빠르게"·"경량"이 있으면 **핵심 모드**, "전체"·"풀"이 있으면 **전체 모드**로 시작한다.
- 인자에 없으면 단계 0 첫 라운드의 첫 문항으로 모드를 묻는다(header "진행 모드", 옵션 2개 — 개선 범위가 넓거나 이해관계자가 많으면 전체에, 단일 자산·마감 촉박이면 핵심에 "(추천)" 표기).
- 결정된 모드를 개선 브리프에 기록하고, 단계 보고마다 현재 모드를 표기한다.
- **게이트(브리프 승인·전략 선택·실물 제작)는 모드와 무관하게 유지된다.** 핵심 모드가 줄이는 것은 내부 작업량뿐이다.
- 사용자가 도중에 전환을 요구하면 전환한다 — 이미 통과한 게이트는 다시 열지 않고, 남은 단계만 새 모드 규칙을 적용한다.
```

- [ ] **Step 2: 단계별 핵심 모드 조항** — 각 단계의 "완료 기준:" 줄 바로 앞에 삽입:

단계 0:
```
**핵심 모드**: 자산 인벤토리는 동일하게 전수 수행하되, 인터뷰 라운드는 1회(최대 4문항)로 제한한다 — BRIEF-INTERVIEW.md 핵심 모드 조항 준용.
```
단계 1:
```
**핵심 모드**: 검수 서브에이전트 1개가 두 축을 순차 진단한다(보고는 축별 분리 유지).
```
단계 2:
```
**핵심 모드**: 기본 생략한다. 브리프가 경쟁·트렌드 목표를 명시한 경우에만 design-researcher 1개로 경량 실행(상한 100줄)한다.
```
단계 3:
```
**핵심 모드**: design-strategist 1개가 개선 강도 1안(브리프의 개선 강도 허용치 기준으로 선택) + 반대 강도 미니 대안(10줄 요약)을 작성한다. 선택 게이트는 유지한다.
```
단계 4:
```
**핵심 모드**: 위임 프롬프트에 분량 상한 200줄을 명시한다. before/after 매핑 의무는 모드와 무관하게 유지된다.
```
단계 5:
```
**핵심 모드**: 검수 서브에이전트 1개가 축 A→B→C를 순차 점검한다(보고는 축별 분리, Critical만 수정 루프).
```
단계 6:
```
**핵심 모드**: 경량화하지 않는다 — 전체 모드와 동일하게 수행한다.
```

- [ ] **Step 3: 합리화 표에 행 추가** — 표 마지막 행 뒤:

```
| "핵심 모드니까 게이트도 줄이자" | 핵심 모드가 줄이는 것은 내부 작업량이다. 게이트는 모드와 무관하게 유지된다. |
```

- [ ] **Step 4: 검증**

Run: `grep -c "핵심 모드" skills/gx-redesign/SKILL.md`
Expected: 10 이상.

- [ ] **Step 5: 커밋**

```bash
git add skills/gx-redesign/SKILL.md
git commit -m "feat: gx-redesign에 전체/핵심 모드 분기 추가

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 8: creative-production·creative-review 스킬 강화

**Files:**
- Modify: `skills/creative-production/SKILL.md`
- Modify: `skills/creative-review/SKILL.md`

**Interfaces:**
- Consumes: Task 1의 PROMPT-PLAYBOOK 규격 명칭.
- Produces: "매체별 필수 섹션" 목록 — Task 4 단계 3 핵심 모드 조항이 참조.

- [ ] **Step 1: creative-production — 프롬프트 원칙 교체** — Edit(old → new):

찾기:
```
- 생성형 AI 프롬프트에는 대상, 구성, 스타일, 조명, 재질, 카메라, 비율, 제외 요소를 명시한다.
```
바꾸기:
```
- 생성형 AI 프롬프트는 gx-design 스킬의 PROMPT-PLAYBOOK 규격을 따른다 — 5요소 구조(Subject/Action/Location·Context/Composition/Style), 생성 설정값(종횡비·해상도·길이)의 프롬프트 본문 분리, 컷별 후속 수정 프롬프트 2~3개.
```

- [ ] **Step 2: creative-production — 필수 섹션·분량 절 추가** — "## 제작 원칙" 절 앞에 삽입:

```markdown
## 매체별 필수 섹션 (완성 판정 기준)

아래 섹션이 하나라도 없으면 그 명세는 미완성이다. 자체 검수에서 확인한다.

- 공통: 제작 목표 / 적용 전략 / 제작 명세 / 자체 검수 결과 / 미확정 사항
- BX: 컬러 시스템(hex) / 타이포그래피 / 로고 또는 그래픽 모티프 / 브랜드 애플리케이션 최소 1종
- 그래픽: 레이아웃·그리드 / 색상 / 타이포그래피 / 매체별 사이즈 / (생성형 사용 시) 이미지 생성 프롬프트
- UX/UI: IA 또는 화면 목록 / 화면별 레이아웃 구조 / 컴포넌트·상태 / 디자인 토큰
- 영상: 러닝타임·규격 / 씬별 구성(시간 배분 검산 포함) / 자막·카피 전문 / 편집 가이드

위임 프롬프트에 분량 상한이 명시되면 필수 섹션을 우선 채우고 상한을 지킨다. 상한 안에서는 수치·규칙의 구체성이 산문 설명보다 우선한다.

```

- [ ] **Step 3: creative-review — 실물 검수 기준 추가** — "### 시각 완성도" 블록(마지막 불릿 `- 이미지 스타일이 일관적인가?`) 뒤에 삽입:

```markdown

### 생성·렌더 실물 (실물 이미지가 있는 경우)

- 실물 이미지를 직접 열어 확인했는가?
- AI 아티팩트나 렌더 결함이 없는가? (깨진 글자, 왜곡, 폰트 폴백 흔적)
- 기술 규격이 정확한가? (치수, 해상도, 컬러 값)
- 구성 문제가 없는가? (잘림, 오버랩, 여백 불균형)
- 다른 자산과 스타일이 일관적인가?
- 산출물이 의도와 다른 것으로 읽히지 않는가? (정체성 오독)
```

- [ ] **Step 4: 검증**

Run: `grep -n "PROMPT-PLAYBOOK\|매체별 필수 섹션" skills/creative-production/SKILL.md` → 각 1건 이상.
Run: `grep -n "생성·렌더 실물" skills/creative-review/SKILL.md` → 1건.

- [ ] **Step 5: 커밋**

```bash
git add skills/creative-production/SKILL.md skills/creative-review/SKILL.md
git commit -m "feat: 제작 필수 섹션 체크리스트·실물 검수 기준(QA 4항목) 추가

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 9: creative-producer 에이전트 수정

**Files:**
- Modify: `agents/creative-producer.md`

**Interfaces:**
- Consumes: Task 1·2 문서 경로.

- [ ] **Step 1: 작업 원칙에 2개 불릿 추가** — `- UI 프로토타입 제작 시 frontend-design 스킬이 사용 가능하면 로드해 그 기준을 따른다.` 뒤에 삽입:

```
- 위임 프롬프트에 PROMPT-PLAYBOOK 경로가 전달되면 생성형 프롬프트를 그 규격으로 작성한다.
- 코드 렌더링·프로토타입 실물을 만들면 렌더 결과 이미지를 직접 열어 보고 VISUAL-QUALITY 기준으로 자체 비평·수정한다(위임 시 경로 전달됨). 렌더 품질 로그를 산출물에 포함한다.
```

- [ ] **Step 2: 검증**

Run: `grep -n "PROMPT-PLAYBOOK\|VISUAL-QUALITY" agents/creative-producer.md` → 각 1건.

- [ ] **Step 3: 커밋**

```bash
git add agents/creative-producer.md
git commit -m "feat: creative-producer에 프롬프트 규격·시각 품질 루프 준수 원칙 추가

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 10: README·marketplace 갱신 + 전체 정합성 검증

**Files:**
- Modify: `README.md`
- Modify: `.claude-plugin/marketplace.json`

- [ ] **Step 1: README 갱신** — README.md를 Read로 읽고, 스킬 소개(gx-design/gx-redesign) 부분에 아래 요지의 짧은 절을 기존 톤에 맞춰 삽입한다(문구는 주변 서식에 맞게 조정 가능, 요지는 유지):

```markdown
### 진행 모드

- **전체 모드**: 리서치 축 병렬, 전략 3안, 2축 병렬 검수 — 산출물 범위가 넓은 프로젝트용.
- **핵심 모드**: 인터뷰 1라운드, 리서치·전략·검수 경량화(승인 게이트는 동일 유지) — 단일 산출물·빠른 진행용.
- 호출 시 인자로 지정하거나(`/gx-design 핵심 ...`), 첫 질문에서 선택한다.
```

버전 표기가 있으면 v0.4.0으로 갱신하고, 신규 문서(PROMPT-PLAYBOOK·VISUAL-QUALITY)를 구조 안내에 반영한다.

- [ ] **Step 2: marketplace.json 갱신** — Edit(old → new):

찾기:
```
      "description": "디자인 멀티에이전트 팀 플러그인 (gx-design·gx-redesign 오케스트레이터 + 전문 에이전트 3종 + 방법론 스킬 4종)"
```
바꾸기:
```
      "description": "디자인 멀티에이전트 팀 플러그인 (gx-design·gx-redesign 오케스트레이터 + 전문 에이전트 3종 + 방법론 스킬 4종, 전체/핵심 모드 지원)"
```

- [ ] **Step 3: 전체 정합성 검증**

```bash
grep -rn "PROMPT-PLAYBOOK" skills/ agents/ | wc -l   # 5건 이상
grep -rn "VISUAL-QUALITY" skills/ agents/ | wc -l    # 4건 이상
ls skills/gx-design/PROMPT-PLAYBOOK.md skills/gx-design/VISUAL-QUALITY.md  # 둘 다 존재
grep -rn "](" skills/gx-design/*.md skills/gx-redesign/*.md | grep -o "]([^)]*\.md)" | sort -u  # 나열된 상대 경로가 전부 실재하는지 육안 확인
```

- [ ] **Step 4: 커밋**

```bash
git add README.md .claude-plugin/marketplace.json
git commit -m "docs: v0.4.0 — 진행 모드 안내·신규 문서 반영

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

## 수용 확인 (전 태스크 완료 후)

1. 스펙 §7 변경 파일 매트릭스의 12개 항목이 전부 반영되었는가.
2. `git log --oneline main..HEAD` — 태스크별 커밋 존재.
3. 드라이런(스펙 §10-2)과 프롬프트 실사용 검증(§10-3)은 병합 후 사용자와 함께 별도 수행한다.
